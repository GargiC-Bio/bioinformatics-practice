#This is my first Python program
# get DNA sequence:
dna=input("Enter a DNA sequence, please:") 
no_c = dna.count('c') # count C's in DNA sequence
no_g = dna.count('g') # count G's in DNA sequence
dna_length = len(dna) # get the length of the DNA sequence 
gc_percent = (no_c+no_g)*100/dna_length # compute GC percentage
print("The DNA sequence's GC content is %5.4f%%:"%gc_percent) # print GC% to screen

# Advance version
def gc(dna) :
    "this function computes the GC percentage of a dna sequence"
    nbases=dna.count('n')+dna.count('N')
    gcpercent=float(dna.count('c')+dna.count('C')+dna.count('g')+dna.count('G'))*100/(len(dna)-nbases)
    return gcpercent
# Get a DNA sequence
dna_data = input("Enter DNA sequence:")

# Call the function and print the result
result = gc(dna_data)
print("The DNA sequence's GC percentage is %5.4f%%:"%result) # print GC% to screen # print GC% to screen

  help(gc)
